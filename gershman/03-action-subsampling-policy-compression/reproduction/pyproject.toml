[project]
name = "action-subsampling-policy-compression-repro"
version = "0.1.0"
description = "Simulation reproducing Liu & Gershman 2025 action-subsampling / policy-compression results"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.0",
    "numpy>=2.5.1",
]
