import { Tex } from "@/primitives/Tex";

export const slides = [
  <section className="flex h-full w-full flex-col items-center justify-center gap-4 p-12">
    <p className="font-mono text-xs tracking-[0.2em] text-accent uppercase">
      Samuel Gershman Lab
    </p>
    <h1 className="max-w-3xl text-center text-5xl font-medium">
      Neural evidence that humans reuse strategies
    </h1>
    <p className="font-mono text-sm text-muted-foreground italic">Placeholder deck</p>
  </section>,
  <section className="flex h-full w-full flex-col items-center justify-center gap-4 p-12">
    <p className="font-mono text-xs tracking-[0.2em] text-accent uppercase">The question</p>
    <p className="max-w-2xl text-center text-3xl leading-snug">
      Do people reuse previously learned control strategies rather than relearning
      from scratch in each new task?
    </p>
  </section>,
  <section className="flex h-full w-full flex-col items-center justify-center gap-4 p-12">
    <p className="font-mono text-xs tracking-[0.2em] text-accent uppercase">
      A policy, formally
    </p>
    <Tex expr="\pi(a \mid s) \propto \exp(Q(s, a) / \tau)" block />
  </section>,
];
