import { Tex } from "@/primitives/Tex";

export const slides = [
  <section className="flex h-full w-full flex-col items-center justify-center gap-4 p-12">
    <h1 className="text-4xl font-bold">Neural evidence that humans reuse strategies</h1>
    <p className="text-gray-500">Gershman Lab — Month 1, Lecture 1 (placeholder)</p>
  </section>,
  <section className="flex h-full w-full flex-col items-center justify-center gap-4 p-12">
    <h2 className="text-3xl font-bold">The question</h2>
    <p className="max-w-2xl text-xl">
      Do people reuse previously learned control strategies rather than relearning
      from scratch in each new task?
    </p>
  </section>,
  <section className="flex h-full w-full flex-col items-center justify-center gap-4 p-12">
    <h2 className="text-3xl font-bold">A policy, formally</h2>
    <Tex expr="\pi(a \mid s) \propto \exp(Q(s, a) / \tau)" block />
  </section>,
];
